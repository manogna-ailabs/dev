// Logic Layer — business rules and system behaviour. Pure functions, no UI or storage.

/**
 * Example: replace this with the core loop logic for your app.
 * e.g. XP calculation, streak logic, recommendation logic, etc.
 */
export function calculateScore(actions: number, multiplier: number): number {
  return actions * multiplier;
}
